project_id = "wfyvnzcnhmrsheivbgip"

[auth]
enabled = true
site_url = "https://user-scan-hub.lovable.app"

[db]
schema = "public"

# All functions now require authentication by default
# Only webhook endpoints or public APIs should disable JWT